import styled from "styled-components"

export const ImageGridCardContainer = styled.div`
  position: fixed;
  top: 50%;
  left: 50%;
  min-width: calc(100vw - 15rem);
  height: calc(100vh - 15rem);
  transform: translate(-50%, -50%);
`
